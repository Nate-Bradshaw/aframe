I've copied over literally every file for the time being into this folder for testing purposes.

Each cursor object has been commented out as there needs to be only one to interact with stuff.

Pages do load dynamically at least.

A-Frame loads in each page also commented out as only needs to be loaded once by test.html.